# Tasks: Authorable cached formula values (round-trip)

Ordered by dependency. Verifiable against `specs/cached-formula-value/spec.md`; implementation
details live in `design.md`. `set_value` impact first per GitNexus rules — `cached_value`
getter, `CellValue::formula`, and the recalc path are **read-only** in this change; the writer
`"Formula"` arm is **edited** (adds the `date_serial` branch, §1.4). Lowest-risk slice.

## 1. Setter — fold cached scalar fields into the Formula CellValue

- [x] 1.1 In `src/model/cell.rs` `set_value`, branch-3 (`obj.get("formula")`), after
      `CellValue::formula(f.to_string())` fold `number`/`string`/`boolean`/`error_value`/
      `date_serial` from the input object (snippet in `design.md` §2). No new `CellValue` fields.
- [x] 1.2 `cargo build` (default features) and `cargo build --features formula-eval` both compile.
- [x] 1.3 GitNexus `impact set_value upstream` pre-edit; `detect_changes()` post-edit — confirm
      blast radius stays to `cell.rs` branch-3 + the writer `date_serial` branch (no signature
      change to `CellValue::formula` or `cached_value` getter).
- [x] 1.4 `src/writer/xlsx.rs:1855` `"Formula"` arm: add the `date_serial` `<v>` branch
      (mirrors the `Date` arm at `xlsx.rs:1916`; the arm already emits `<v>` for cached
      `number`/`string`/`boolean`/`error_value`).
- [x] 1.5 Issue #55 (TS decl gap): `index.d.ts` `class Cell` — add
      `get cachedValue(): CellValue | null` (runtime getter already exists at `cell.rs:459`
      under `#[napi(getter, js_name="cachedValue")]`; this is a 1-line TS-only addition).
      Mirror in `native.d.ts`.

## 2. TypeScript surface — widen the Formula union

- [x] 2.1 `index.d.ts`: `CellValue` and input `CellValueInput` `Formula` arm gains optional
      `number?`, `string?`, `boolean?`, `errorValue?`, `dateSerial?`.
- [x] 2.2 `native.d.ts`: same widening applied by hand (mirror of `index.d.ts` — `apply-glue.cjs`
      does not regenerate it; see `design.md` Risks §3).
- [x] 2.3 Confirm `apply-glue.cjs` rewrite of `getCell`/union still runs green; add an
      Open Question / follow-up note: lint `index.d.ts` vs `native.d.ts` parity (OQ3).

## 3. Fixtures

- [x] 3.1 Generate ExcelJS-authored cached formula: `cell.value = { formula: "A2+B2",
      result: 3 }` → commit under `openspec/.../fixtures/` (proves ExcelJS emits `<v>`
      via `cell-xform.js:158-189`, debunks issue #54 assumption).
- [x] 3.2 Hand-craft a ~200-byte `.xlsx` carrying `<f>A2+B2</f><v>3</v>` to isolate the
      **pure read path** from the writer/authorship code; commit it.
- [x] 3.3 Copy both into `__test__/fixtures/`.
- [x] 3.4 Inspect the generated bytes (`unzip -l` / `strings`) to confirm `<f>` + `<v>` both
      present (regression lock on the read path).

## 4. Tests

- [x] 4.1 `__test__/reader.test.ts` / new `cached-formula.test.ts`: ExcelJS-authored `{ formula,
      result: N }` round-trips to `cell.value === N` and `cell.formula` present
      (spec scenario "numeric cached value round-trips").
- [x] 4.2 Assert the boolean / string / error / date scalar round-trips
      (spec §ADDED Req 1 scenarios).
- [x] 4.3 Assert the committed hand-crafted `.xlsx` reads back `cell.value === 3` +
      `cell.formula === "A2+B2"` (spec Req 2 / "Excel-authored cached formula reads back").
- [x] 4.4 **No regression**: a formula authored *without* a cache (`{ formula: "SUM(A1:B1)" }`)
      still reads back `cell.value === null` + `cell.formula === "SUM(A1:B1)"`
      (spec scenario "formula authored without a cached value still reads back").
- [x] 4.5 Existing `reader.test.ts:163` / `rich-text` formula tests still pass unchanged.
- [x] 4.6 Rust `cargo test` (incl. `formula/tests.rs` recalc tests) — **assert** the
      `cached_value` getter behavior is unchanged (spec §R4: still `null` for non-Formula-typed
      cells; recalc path unchanged).

## 5. Docs & changelog

- [x] 5.1 `docs/spec.md` §10 (+ `ROADMAP.md` if it lists cached-formula round-trip): note
      user/Excel-supplied cached results are now authorable round-trippable.
- [x] 5.2 `CHANGELOG.md` entry keyed under the formula-cached-result behavior
      (D1a preserved: `cachedValue` getter unchanged).

## Verification

- `cell.value` === cached scalar (typed) for author+Excel cached formula cells — spec §R1–R3.
- `cell.formula` preserved — spec §R1–R3, R4.
- `cell.cachedValue` behavior **unchanged** (still recalc-only) — spec §R4.
- No `CellValue` signature / getter Rust edits — `detect_changes()` shows only
  `cell.rs` branch-3 + writer `date_serial` branch + test/fixture/doc files.
- `applyRequires` for implementation = `tasks` (proposal + spec + design already present).

## Deferred / explicit non-tasks

- D1b `cachedValue` getter unification (OQ1) — `design.md`.
- ExcelJS `{ formula, result }` object `cell.value` alignment (OQ2) — `design.md`.
- `apply-glue.cjs` parity lint (OQ3) — noted as follow-up infra task, **not** done here.
